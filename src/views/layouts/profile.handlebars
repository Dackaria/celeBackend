<div>
  <h1>Perfil de usuario</h1>

  <p>{{first_name}}</p>
  <p>{{last_name}}</p>
  <p>{{email}}</p>

  <div>
    <form action="/api/session/logout" method="get">
      <br />
      <button type="submit">logout</button>
    </form>
  </div>

</div>